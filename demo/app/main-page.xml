<Page xmlns="http://schemas.nativescript.org/tns.xsd" 
      navigatedTo="onPageNavigatedTo"  >
      <ScrollView>
         <StackLayout>
            <Button class="btn" text="sample usage" nav="sample-usage" tap="goto" />
            <Button class="btn" text="work with Repeater" nav="with-repeater" tap="goto" />
            <Button class="btn" text="android like tabview" nav="tab-view" tap="goto" />
            <Button class="btn" text="intro slides with indicators" nav="intro-slides" tap="goto" />
         </StackLayout> 
      </ScrollView>
</Page>